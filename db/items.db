CREATE TABLE category (
  id INTEGER PRIMARY KEY,
  name TEXT UNIQUE
);
CREATE TABLE items (
  id INTEGER PRIMARY KEY,
  name TEXT,
  category_id INTEGER,
  image_filename TEXT,
  FOREIGN KEY (category_id) REFERENCES category(id)
);

